# Yuna - Example Environment Variables
# Copy this file to `.env` and adjust as needed.
# These defaults work with `docker compose up -d` using the included docker-compose.yml.

# Database
# Prisma uses DATABASE_URL to connect. The host `db` matches the docker compose service name.
DATABASE_URL=postgresql://yuna:yuna@db:5432/yuna?schema=public
POSTGRES_PORT=5432
POSTGRES_USER=yuna
POSTGRES_PASSWORD=yuna
POSTGRES_DB=yuna

# API
# The API runs on this port and is published by docker compose.
API_PORT=8080
# One of: development | production | test
NODE_ENV=development
# One of: trace | debug | info | warn | error | fatal | silent
LOG_LEVEL=debug

# Redis
# The host `redis` matches the docker compose service name.
REDIS_HOST=redis
REDIS_PORT=6379

# Updater / Health
# Interval in seconds to check for updates (set 0 to disable if implemented)
UPDATE_CHECK_INTERVAL=3600
# Path used by the API healthcheck
HEALTHCHECK_PATH=/health
